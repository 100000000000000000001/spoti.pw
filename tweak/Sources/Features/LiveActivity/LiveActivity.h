// The playing track and the line being sung as a Live Activity on the lock screen and in the Dynamic
// Island, with previous, play/pause and next (iOS 17+). The lines and the clock come from Karaoke, the
// widget is extension/LiveActivity, built into the IPA by scripts/build-extension.sh.
#import <Foundation/Foundation.h>

#define SGKeyLiveActivity @"spotifyglass.liveActivity"
